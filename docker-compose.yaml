# docker-compose.yaml

services:
  frontend:
    image: nerkhbaan-frontend
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: nerkhbaan-frontend
    # Map port 80 inside the container to port 3000 on the host loopback interface
    ports:
      - "127.0.0.1:3000:80"
    restart: unless-stopped
    depends_on:
      - backend

  backend:
    build:
      context: ./apps/api
    container_name: nerkhbaan-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://nerkhbaan:${POSTGRES_PASSWORD}@postgres:5432/nerkhbaan
      API_HOST: 0.0.0.0
      API_PORT: 8000
      # Ensure your production domain is allowed for CORS
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
      ALANCHAND_API_TOKEN: ${ALANCHAND_API_TOKEN:-}
    # Map port 8000 inside the container to port 8000 on the host loopback interface
    ports:
      - "127.0.0.1:8000:8000"
      
  postgres:
    image: postgres:16-alpine
    container_name: nerkhbaan-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: nerkhbaan
      POSTGRES_USER: nerkhbaan
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nerkhbaan -d nerkhbaan']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

